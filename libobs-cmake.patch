--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -15,7 +15,11 @@ if ("${CMAKE_SYSTEM_NAME}" MATCHES "Linux")
     add_definitions(-DUNIX=1)
 endif ()

-find_package(LibObs REQUIRED)
+# Allow manual specification of libobs paths for NixOS
+if(NOT LIBOBS_INCLUDE_DIR)
+  find_package(LibObs REQUIRED)
+endif()
+set(LIBOBS_LIBRARIES obs)

 set(image-reaction_SOURCES image-reaction.c)
